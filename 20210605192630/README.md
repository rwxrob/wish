# Speech to WeeChat from Command Line

I really want a way that my mic can be picked up (only) and converted to
text chat that appears in `weechat` (or whatever I can pipe it to from
the command line).
